CREATE TABLE `drugs` (
	`no` integer PRIMARY KEY NOT NULL,
	`brandName` text,
	`age` integer,
	`genericName` text,
	`dosageFormName` text,
	`strength` text,
	`packSize` text,
	`companyName` text,
	`countryOfOrigin` text,
	`agentName` text
);
